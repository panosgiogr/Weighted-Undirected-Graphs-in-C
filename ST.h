#include "GraphTypes.h"
void Initialize(Graph*);
void InsertEdge(Vertex, Vertex, Weight ,Graph*);
void Print(Graph*);
void BreadthFirst(Graph,Vertex);
void DestroyGraph(Graph*);
void Kruskal (Graph*);